const { LINE_MODE_MULTIPLIER } = require("blockly")

/**
 * ブロックエディタ
 */
module.exports = class BlockEditor {
    constructor() {
    }

    /**
     * エディタに現在参照しているファイルの情報をバインド
     */
    bind(){
        // エディタに現在参照しているファイルの情報をバインド後のイベント
        // Event.BlockEditor.bind
    }

}
